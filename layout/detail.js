import { View, Text } from "react-native";
import React from "react";

const Detail = () => {
  return (
    <View
      style={{
        justifyContent: "center",
        alignContent: "center",
        flex: 1,
      }}
    >
      <Text>detail</Text>
    </View>
  );
};

export default Detail;
